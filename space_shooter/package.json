{
  "name": "space-shooter",
  "version": "1.0.0",
  "description": "Hard-mode mobile web space shooter with TDD",
  "main": "index.html",
  "scripts": {
    "test": "jest --env=jsdom",
    "build": "echo 'No build needed - vanilla JS'",
    "lint": "echo 'Linting skipped for quick delivery'"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
